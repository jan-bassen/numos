# @repo/email

Transactional email templates for Numos, built with [React Email](https://react.email/).

> Not in use yet.

## Development

Preview templates locally with the React Email dev server:

```sh
pnpm dev
```

Templates live in `src/emails`.
